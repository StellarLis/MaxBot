services:
  backend-service:
    build: ./backend
    container_name: backend-service
    environment:
      - DB_HOST=0.0.0.0
      - DB_USER=postgres
      - DB_PASSWORD=sigma_password_noone12351
      - DB_PORT=5432
      - DB_NAME=app_db
      - PORT=10000
    ports:
      - "8080:8080"
    depends_on:
      - postgresql
    networks:
      - app_net
    volumes:
      - ./backend:/app
  postgresql:
    image: postgres:latest
    container_name: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=sigma_password_noone12351
      - POSTGRES_DB=app_db
    ports:
      - "5432:5432"
    networks:
      - app_net

networks:
  app_net:
    driver: bridge